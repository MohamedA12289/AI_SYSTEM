import type { ActivityEntry, Approval, ChatHistoryItem, ChatMessage, Document, MemoryEntry, Note, Project, ProjectFile, Secret, Snapshot, Task, TestCase } from "@/types";

export const demoProjects: Project[] = [];
export const demoChatHistory: ChatHistoryItem[] = [];
export const demoProjectMessages: ChatMessage[] = [];
export const demoFiles: ProjectFile[] = [];
export const demoSecrets: Secret[] = [];
export const demoMemory: MemoryEntry[] = [];
export const demoTasks: Task[] = [];
export const demoNotes: Note[] = [];
export const demoActivity: ActivityEntry[] = [];
export const demoSnapshots: Snapshot[] = [];
export const demoDocuments: Document[] = [];
export const demoTests: TestCase[] = [];
export const demoApprovals: Approval[] = [];
export const demoSelfUpgradeFiles: ProjectFile[] = [];
